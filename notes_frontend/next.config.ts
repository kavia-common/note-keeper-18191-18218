import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Using default SSR for dev/start; adjust as necessary for your deployment.
  // Keep the config minimal to avoid disrupting template behavior.
};

export default nextConfig;
